# Historical v1 static-demo placeholders only; these are NOT v2 login credentials.
# The v1 gate was a presentation feature, not secure authentication.
# For current server-only configuration, see DEPLOYMENT_HANDOFF.md.
# Never put a real password, data key or API token in a VITE_ variable.
VITE_DEMO_USERNAME=demo@boveda.dev
VITE_DEMO_PASSWORD=boveda-demo
