# C1 2.10 — Construction introductions

All 23 available audits now have a source-grounded opening explaining preparation, model inputs, learning and the resulting capability or model landscape. It is an editorial synthesis from canonical S3/S4 statements, with its own INTERPRETED status. Every contributing statement retains its canonical status and references; cross-stage references carry their stage to disambiguate matching evidence IDs. The short learning-task field remains unchanged and is the fallback if an introduction cannot be safely composed.

The first pass was refined to avoid dense technical inventories. All selected introductions passed deterministic checks and paired semantic review, followed by manual review. R17 retains a 112-word introduction because the training/holdout distinction is material. Numeric validation now handles Unicode minus signs and Unicode ranges in supporting context without weakening checks against invented values.

The opening text starts closer to “How is it built?” on desktop, using a narrower title column. Mobile retains its stacked layout. All preceding purpose-card, tab, badge, list and R² gauge changes are retained.

Validation: 21 editorial tests; 17 product/revision tests; 20 metric tests; 23 desktop and 23 dark-mobile Construction pages without horizontal overflow; source inspection in the UI; all 550 introduction references resolved. Existing C1 components, Q3, publication and canonical passthrough are unchanged. The exported 23 canonical reports are byte-identical and all 5,304 evidence records are unchanged. No analytical stage was rerun. R10/R20 remain unavailable.

API generation and review usage: $0.926665 over 111 requests, including discarded first-pass and refinement candidates, with no unknown usage. Provider model and effort configuration remain unchanged.

Rollback: previous local activation is preserved in `previous_activation.json`, previous encrypted hosted data in `previous_hosted_data/`, and prior release in Git at 4149490. Qualified new editorial files are kept separately under `qualified/` with source hashes. Local retains New audit; the hosted demo exports all 23 available audits with New audit disabled.
